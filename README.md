# FCS-Artifact
An artifact about EOSIO vulnerabilities and attacks.

- `vulnerabilities` contains the detailed information of 15 vulnerabilities in EOSIO;
- `attacks` contains the corresponding 19 types of attacks;
- `artifacts` contains some manually rechecked vulnerable and safe samples, which can be used as the ground truth for the following research.